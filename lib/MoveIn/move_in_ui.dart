import 'package:flutter/material.dart';

class MoveIn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('MoveIn'), centerTitle: true,),
    );
  }
}